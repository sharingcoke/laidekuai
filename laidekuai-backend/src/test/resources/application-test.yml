spring:
  datasource:
    url: jdbc:h2:mem:laidekuai-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false

logging:
  level:
    root: WARN
    com.laidekuai: INFO
